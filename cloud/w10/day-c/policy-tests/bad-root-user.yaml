apiVersion: v1
kind: Pod
metadata:
  name: bad-root-user
  namespace: demo
spec:
  securityContext:
    runAsUser: 0
  containers:
    - name: web
      image: nginx:1.27
      resources:
        limits:
          cpu: "100m"
          memory: "128Mi"
